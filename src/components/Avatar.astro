---
export type Props = {
  img: string;
  alt: string;
};

const { img, alt } = Astro.props;
---

<div
  class="flex w-[120px] items-center justify-center rounded-full border border-gray-800"
>
  <img src={img} alt={alt} class="flex-grow rounded-full object-contain" />
</div>
