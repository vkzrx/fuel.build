---
export type Props = {
  img: string;
  alt: string;
};

const { img, alt } = Astro.props;
---

<div
  class="flex h-[80px] w-[80px] items-center justify-center rounded-full border border-teal-200/20"
>
  <img src={img} alt={alt} class="flex-grow" />
</div>
